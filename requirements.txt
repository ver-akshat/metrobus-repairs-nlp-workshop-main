Flask
gunicorn

tensorflow  # Updated TensorFlow for Python 3.9 compatibility
scikit-learn
numpy       # Use a version compatible with TensorFlow 2.10.1
pandas
protobuf    # Update to ensure compatibility with newer TensorFlow
Werkzeug     # Updated to match the latest stable release
